# packages/

Reservado para pacotes e *bridges* compartilhados entre os apps do monorepo
(ex.: tipos comuns, SDKs, integrações).

Vazio por ora — criar pacotes só quando houver código realmente compartilhado
entre `apps/` (evitar pacotes especulativos).
